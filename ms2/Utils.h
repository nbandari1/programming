#ifndef SDDS_UTILS_H
#define SDDS_UTILS_H
#include <cstring>
#include <iostream>

namespace sdds {
	void strnCpy(char* des, const char* src, int len);
	int strLen(const char* s);

}
#endif
